```javascript
const draggable = new Draggable( element, {
  start( position ) {
    // touchstart / mousedown
  },
  move( position ) {
    // touchmove / mousemove
  },
  end( position ) {
    // touchend / mouseup
  },
})
```
